[project]
name = "neonodes"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["textual>=0.47.0"]

[project.scripts]
neonodes = "neonodes.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
